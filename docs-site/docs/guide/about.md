# 关于与更新

「设置 → 关于」显示应用信息，也是检查更新的入口。

## 版本与检查更新

顶部卡片显示 App 图标、应用名和一句简介，下面的「版本」行显示当前版本号。

**点版本行手动检查更新**：会去 GitHub 的发布列表查有没有更高版本（按你选的更新通道过滤）。有新版本时弹窗显示「发现新版本」，并列出**从你当前版本到最新版本之间的全部更新日志**，点「前往下载」用浏览器打开 GitHub Releases 页面取 APK。已经是最新版会提示「已经是最新版本」；网络失败会提示「检查失败」并给出错误信息。

## 自动检查更新

默认开启。打开 App 进主页时在后台异步检测，**每天最多一次**（当天检测过就不再重复）。发现新版本会自动弹更新窗，网络失败时静默处理不打扰你。

关掉之后不再自动检测，但手动点版本行仍然可以检查。

## 更新通道

默认「稳定版」：

- **稳定版**：只检测正式版，过滤掉 RC 预览版。
- **最新版**：包含 RC 预览版。

想帮忙测新功能就切到最新版，图稳定就留在稳定版。

## 其它入口

- **GitHub 仓库**：用系统浏览器打开源码仓库。
- **开源许可证**：本项目使用 GPL-3.0，点击打开完整的 LICENSE 文件。
